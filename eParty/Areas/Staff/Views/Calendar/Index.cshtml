@{
    ViewBag.Title = "Lịch làm việc"; // Đổi tiêu đề
    Layout = "~/Areas/Staff/Views/Shared/_LayoutStaff.cshtml"; // Sử dụng layout của Staff
}

<div class="app-container" style="padding-top:70px;padding-left:10px; display:flex; gap:20px;">
    <div style="flex:1;">
        <h1><i class="fas fa-calendar-alt"></i> Lịch làm việc</h1>
        <div class="header-controls" style="display:flex;align-items:center;gap:15px;margin-bottom:15px;">
            <button id="today-btn" style="padding:5px 10px;">Hôm nay</button>

            <div class="view-options">
                <button class="view-option active" data-view="day">Ngày</button>
                <button class="view-option" data-view="week">Tuần</button>

                <button class="view-option" data-view="month">Tháng</button>
            </div>

            <div class="date-navigation" style="display:flex;align-items:center;gap:10px;">
                <button id="prev-btn"><i class="fas fa-chevron-left"></i></button>
                <h2 id="current-date" style="margin:0;">Loading...</h2>
                <button id="next-btn"><i class="fas fa-chevron-right"></i></button>

            </div>
        </div>

        <div class="calendar-view" id="calendar-view" style="border:1px solid #ddd;
min-height:400px; padding:10px;">
            @* Nội dung lịch sẽ được tải vào đây bằng JavaScript *@
        </div>
    </div>

    @* Phần sidebar sự kiện sắp tới - có thể giữ lại hoặc xóa nếu không cần thiết cho Staff *@
    @*
            <div style="width:300px;
        border-left:1px solid #ddd; padding:10px;">
                <button id="toggle-sidebar" class="btn btn-sm btn-secondary mb-2">Ẩn/Hiện Sự kiện</button>
                <div id="event-sidebar">

           <h3>Sự kiện Sắp tới</h3>
                    <div id="events-list" style="min-height:200px;">
                        <p>Đang tải sự kiện...</p>
                    </div>
                </div>
            </div>
    *@
</div>

@* Nút "Add Event" đã bị xóa vì đây là view chỉ xem *@
@* <button id="add-event-btn" ...>Add Event</button> *@

@* Modal thêm/sửa sự kiện đã bị xóa *@
@* <div id="event-modal" ...>...</div> *@

@* Modal chi tiết sự kiện - Giữ lại để xem thông tin, các nút sửa/xóa sẽ bị ẩn bởi script.js *@
<div id="event-details-modal" class="modal" style="display:none;
position: fixed; z-index: 1050; left: 0; top: 0; width: 100%; height: 100%; overflow: auto; background-color: rgba(0,0,0,0.4); justify-content: center;
align-items: center;">
    <div class="modal-content" style="background:#fff; padding:20px; border-radius:8px; width:90%; max-width: 450px; position:relative;
box-shadow: 0 5px 15px rgba(0,0,0,.5);">
        <span class="close-btn" style="position:absolute; top:10px; right:15px; cursor:pointer; font-size: 24px;
font-weight: bold;">&times;</span>
        <h3 id="details-title" class="mb-3">Event Title</h3>
        <hr />
        <p><i class="fas fa-calendar-alt me-2 text-primary"></i> <strong id="details-date">Date</strong></p>
        <p><i class="fas fa-clock me-2 text-primary"></i> <strong id="details-time">Time</strong></p>
        <p class="mt-3"><strong class="d-block mb-1">Mô tả:</strong> <span id="details-description">Description goes here...</span></p>
        <hr />
        <div class="d-flex justify-content-end gap-2 mt-3">
            @* Các nút này sẽ được ẩn/hiện bởi script.js dựa trên biến isCalendarReadOnly *@
            <button id="edit-event-btn" class="btn
btn-warning">
                <i class="fas fa-edit me-1"></i> Sửa
            </button>
            <button id="delete-event-btn" class="btn btn-danger"><i class="fas fa-trash me-1"></i> Xóa</button>
            <button id="close-details-btn" class="btn btn-secondary close-btn">Đóng</button>
        </div>
    </div>
</div>


@section Styles {
    <link rel="stylesheet" href="~/Scripts/admin/calendar/style.css" /> @* Sử dụng lại CSS của Admin *@
    <style>
                /* *** SỬA LỖI (Patch #4): Định nghĩa chiều cao giờ ở một nơi *** */
                :root {
                    --hour-h: 50px;
                    /* Đặt chiều cao 1 giờ là 50px */
                }

                .modal {
                    z-index: 1055 !important; /* Đảm bảo modal hiển thị trên layout */
                }

                .modal-content {
                    max-height: 90vh;
                    overflow-y: auto;
                }

                .form-control-color {
                    width: 50px;
                    height: 38px;
                    padding: .1rem .3rem;
                }

                /* CSS ẩn/hiện sidebar (nếu bạn giữ lại sidebar) */
                #event-sidebar:not(.open) {
                    /* display: none; */ /* Hoặc dùng class để ẩn thay vì JS */
                }


                /* --- CSS CHUẨN CHO DAY VIEW (Copy từ Admin View) --- */
                .day-grid {
                    position: relative;
                    display: grid;
                    grid-template-columns: 60px 1fr;
                    grid-template-rows: repeat(24, var(--hour-h));
                    border-top: 1px solid #eee;
                }

                .events-overlay {
                    position: absolute;
                    top: 0;
                    left: 60px;
                    right: 0;
                    bottom: 0;
                    pointer-events: none;
                }


                .day-event-abs {
                    position: absolute;
                    box-shadow: 0 1px 3px rgba(0,0,0,.15);
                    color: #fff;
                    font-size: 12px;
                    line-height: 1.2;
                    overflow: hidden;
                    text-overflow: ellipsis;
                    white-space: nowrap;
                    z-index: 1;
                    left: 5px;
                    right: 5px;
                    border-radius: 4px;
                    padding: 2px 5px;
                    cursor: pointer;
                    pointer-events: auto;
                    opacity: 0.9;
                    border: 1px solid rgba(0,0,0,0.1);
                }

                    .day-event-abs:hover {
                        opacity: 1;
                    }

                .hour-label {
                    height: var(--hour-h);
                    text-align: right;
                    padding-right: 10px;
                    font-size: 0.8em;
                    color: #666;
                    border-right: 1px solid #eee;
                    border-bottom: 1px solid #eee;
                    box-sizing: border-box;
                    position: relative;
                    line-height: var(--hour-h);
                    padding-top: 0;
                }

                .day-time-block {
                    height: var(--hour-h);
                    border-bottom: 1px solid #eee;
                    box-sizing: border-box;
                }


                /* --- CSS CHUẨN CHO WEEK VIEW (Copy từ Admin View) --- */
                .week-view {
                    width: 100%;
                    overflow-x: auto;
                }

                .week-header {
                    display: grid;
                    grid-template-columns: 60px repeat(7, 1fr);
                    border-bottom: 1px solid #ddd;
                    min-width: 800px;
                }

                .hour-label-corner {
                    height: auto;
                    padding-bottom: 5px;
                    border-right: 1px solid #eee;
                    border-bottom: 1px solid #eee;
                    min-width: 60px;
                    box-sizing: border-box;
                }

                .week-day-header {
                    text-align: center;
                    padding: 5px 0;
                    font-weight: 600;
                    font-size: 0.9em;
                    border-right: 1px solid #eee;
                    border-bottom: 1px solid #eee;
                    min-width: 100px;
                    box-sizing: border-box;
                }

                    .week-day-header:last-child {
                        border-right: none;
                    }

                .week-day-number {
                    font-size: 1.1em;
                    margin-top: 2px;
                }

                .week-day-header.current-day .week-day-number {
                    background-color: #4e73df;
                    color: white;
                    border-radius: 50%;
                    width: 24px;
                    height: 24px;
                    line-height: 24px;
                    display: inline-block;
                }

                .week-grid {
                    display: grid;
                    grid-template-columns: 60px repeat(7, 1fr);
                    grid-template-rows: repeat(24, var(--hour-h));
                    min-width: 800px;
                }

                    .week-grid > .hour-label {
                        grid-column: 1 / 2;
                        height: var(--hour-h);
                        text-align: right;
                        padding-right: 10px;
                        font-size: 0.8em;
                        color: #666;
                        border-right: 1px solid #eee;
                        border-bottom: 1px solid #eee;
                        box-sizing: border-box;
                        position: relative;
                        line-height: var(--hour-h);
                        min-width: 60px;
                    }

                .week-time-block {
                    position: relative;
                    height: var(--hour-h);
                    border-bottom: 1px solid #eee;
                    border-right: 1px solid #eee;
                    box-sizing: border-box;
                    min-width: 100px;
                }

                    .week-time-block:nth-child(8n) { /* Cột cuối cùng không có border phải */
                        border-right: none;
                    }

                .week-event {
                    position: absolute;
                    color: #fff;
                    box-shadow: 0 1px 3px rgba(0,0,0,.15);
                    cursor: pointer;
                    z-index: 1;
                    left: 2px;
                    right: 2px;
                    background-color: #4e73df;
                    opacity: .9;
                    border-radius: 4px;
                    font-size: 11px;
                    padding: 1px 3px;
                    overflow: hidden;
                    text-overflow: ellipsis;
                    white-space: nowrap;
                    pointer-events: auto;
                    border: 1px solid rgba(0,0,0,0.1);
                }

                    .week-event:hover {
                        opacity: 1;
                    }


                /* Styles Month View (Copy từ Admin View) */
                .month-view {
                    width: 100%;
                    box-sizing: border-box;
                }

                .month-header {
                    display: grid;
                    grid-template-columns: repeat(7, 1fr);
                    text-align: center;
                    font-weight: 600;
                    margin-bottom: 0.5rem;
                }

                .month-days {
                    display: grid;
                    grid-template-columns: repeat(7, 1fr);
                    gap: 2px; /* Giảm khoảng cách nếu cần */
                }

                .day-header {
                    padding: 0.5rem 0.25rem;
                    text-align: center;
                    font-weight: 600;
                    color: var(--text-color, #5a5c69);
                    font-size: 0.9em;
                }

                .day-cell {
                    aspect-ratio: 1/1; /* Giữ ô vuông */
                    min-height: 100px; /* Chiều cao tối thiểu */
                    padding: 0.25rem;
                    border: 1px solid var(--border-color, #e3e6f0);
                    border-radius: 4px;
                    cursor: default; /* Đổi thành default cho read-only */
                    transition: all 0.2s;
                    position: relative;
                }
                /* Bỏ hiệu ứng hover nếu không cần */
                /* .day-cell:hover {
                        background-color: var(--secondary-color);
        } */

                .day-number {
                    font-weight: 600;
                    margin-bottom: 0.25rem;
                    font-size: 0.85em;
                }

                .day-events {
                    overflow-y: auto;
                    max-height: calc(100% - 1.5rem); /* Chiều cao tối đa cho events */
                }

                .day-event {
                    font-size: 0.75rem;
                    padding: 0.15rem 0.25rem;
                    border-radius: 0.15rem;
                    margin-bottom: 0.15rem;
                    white-space: nowrap;
                    overflow: hidden;
                    text-overflow: ellipsis;
                    color: white;
                }

                .more-events {
                    font-size: 0.7em;
                    color: #666;
                    text-align: center;
                    margin-top: 2px;
                }

                .other-month .day-number, .other-month .day-events {
                    opacity: 0.5; /* Làm mờ ngày tháng khác */
                }

                .current-day .day-number {
                    background-color: var(--primary-color, #4e73df);
                    color: white;
                    border-radius: 50%;
                    width: 24px;
                    height: 24px;
                    line-height: 24px;
                    text-align: center;
                    margin: 0 auto 0.25rem;
                }
    </style>
}

@section Scripts {
    <script>
        // Lấy dữ liệu events từ ViewBag và biến chỉ xem
        var serverEvents = @Html.Raw(ViewBag.ServerEventsJson ?? "[]");
        var isCalendarReadOnly = @(ViewBag.IsReadOnly != null && ViewBag.IsReadOnly ? "true" : "false"); // Truyền biến chỉ xem
console.log("Events loaded for staff:", serverEvents, "Read Only:", isCalendarReadOnly);

        @if (ViewBag.CalendarError != null)
        {
            <text>
            // Hiển thị lỗi nếu có
            document.addEventListener('DOMContentLoaded', function() {
                console.error("@ViewBag.CalendarError");
                var calendarViewEl = document.getElementById('calendar-view');
                if(calendarViewEl) {
                     calendarViewEl.innerHTML = '<div class="alert alert-danger">@ViewBag.CalendarError</div>';
                }
                var eventListEl = document.getElementById('events-list'); // Nếu bạn giữ lại sidebar
                if(eventListEl) {

              eventListEl.innerHTML = '<p style="color: red;">@ViewBag.CalendarError</p>';
                }
            });
</text>
        }
    </script>

    @* Giữ nguyên script.js vì nó đã được cập nhật để xử lý isCalendarReadOnly *@
    <script src="~/Scripts/admin/calendar/script.js"></script>

    @* Script bổ sung (nếu cần) có thể đặt ở đây *@
    @*
        <script>
            document.addEventListener('DOMContentLoaded', function () {
                // Thêm các tùy chỉnh chỉ dành cho Staff nếu cần
                if (isCalendarReadOnly) {
                     console.log("Staff calendar initialized in read-only mode.");
                     // Ví dụ: Thay đổi màu nền
                     // const calendarContainer = document.querySelector('.app-container');
                     // if (calendarContainer) calendarContainer.style.backgroundColor = "#f0f0f0";
                }
            });
        </script>
    *@
}